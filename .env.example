# ─────────────────────────────────────────────────────────────────────────────
# Configuración de entorno del Chatbot — Alcaldía de Floridablanca
#
# IMPORTANTE: Vite incrusta el valor de TODA variable que empiece por `VITE_`
# literalmente dentro del JavaScript compilado. `dist/assets/*.js` es un archivo
# estático público, así que cualquier persona puede descargarlo y leer esos valores.
#
# Regla: aquí solo van datos que puedan ser públicos (URLs de servicios).
# NUNCA claves de API, tokens ni contraseñas.
# ─────────────────────────────────────────────────────────────────────────────

# URL base del microservicio RPA de PQRSD.
# Usa https:// en producción: por aquí viajan documento, teléfono y correo del ciudadano.
VITE_RPA_PQRSD_API_URL=http://localhost:8000

# URL base del microservicio RPA de Impuesto Predial.
VITE_RPA_PREDIAL_API_URL=http://localhost:8000


# ─────────────────────────────────────────────────────────────────────────────
# VITE_GEMINI_API_KEY — DELIBERADAMENTE AUSENTE
#
# La aplicación ya NO lee esta variable, y no debe volver a hacerlo.
#
# Antes se leía como valor por defecto de la clave, lo que la publicaba en claro
# dentro del bundle de producción (verificado: aparecía en `dist/assets/*.js`).
# Cualquier visitante podía extraerla y consumir la cuota de la Alcaldía.
#
# La clave se introduce ahora desde el panel de control del chatbot y queda
# únicamente en el navegador de quien la escribe.
#
# Aun así, como la llamada a Gemini se hace directamente desde el cliente, la clave
# sigue siendo visible para quien use ese navegador. Eso no se puede resolver desde
# el frontend. Medidas necesarias en Google Cloud Console:
#
#   1. Restringir la clave por referente HTTP (solo los dominios de la Alcaldía).
#   2. Restringirla a la API "Generative Language" únicamente.
#   3. Fijar una cuota diaria baja, para acotar el gasto si se filtra.
#   4. Rotarla si alguna vez se desplegó un build que la contenía.
#
# Solución definitiva: un proxy en el backend que guarde la clave del lado del
# servidor. La arquitectura ya está preparada — ver `src/ports/AiProviderPort.js`:
# basta con registrar un adaptador nuevo, sin tocar el resto de la aplicación.
# ─────────────────────────────────────────────────────────────────────────────

# Nota sobre `IMPORTANTE` de arriba: al añadir variables nuevas, léelas SIEMPRE con
# acceso estático — `import.meta.env.VITE_MI_VAR`. Un acceso dinámico como
# `import.meta.env[nombre]` impide la sustitución en tiempo de compilación y hace que
# Vite incruste el objeto de entorno COMPLETO, filtrando todas las variables.
